/**
 * Identity service-specific types.
 * Types that are internal to this service and not exposed via the public API.
 */

/**
 * Maximum allowed access token lifetime in seconds (60 minutes).
 */
export const MAX_ACCESS_TOKEN_LIFETIME_SECONDS = 3600;

/**
 * Maximum allowed refresh token lifetime in seconds (24 hours).
 */
export const MAX_REFRESH_TOKEN_LIFETIME_SECONDS = 86400;
